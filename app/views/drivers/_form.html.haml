= simple_form_for @driver, :html => { :class => 'form-horizontal' } do |f|
  = f.input :name
  = f.input :username
  = f.input :code
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), drivers_path, :class => 'btn'
